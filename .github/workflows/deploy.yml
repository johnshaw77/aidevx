name: Deploy to Server

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: 152.42.160.234
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: /usr/local/bin/deploy-aidevx
